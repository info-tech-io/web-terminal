{
  "name": "tps-widget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/widget.js --bundle --minify --outfile=widget.js --platform=browser --target=es2017 --loader:.css=text",
    "build:dev": "esbuild src/widget.js --bundle --outfile=widget.js --platform=browser --target=es2017 --sourcemap --loader:.css=text"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
