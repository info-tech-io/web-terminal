# Terminal Pool Service — configuration
# Copy to .env and adjust values

# Docker image name for the terminal container
TPS_IMAGE_NAME=tps-base

# Docker container name (single-instance mode, Phase 0)
TPS_CONTAINER_NAME=tps-instance

# Server bind address
TPS_HOST=0.0.0.0

# Server port
TPS_PORT=8080
